- if @shop.user == current_user
  = content_for :sidebar do
    %nav.shop-panel
      %h6= Spree.t(:your_shop_panel)
      %ul
        %li= "#edit"
        %li= "#accouting"


.shop-show
  - @body_id = "shop-details"
  .columns.ten.omega
    .row
      = @shop.email
      %p= @shop.user.email